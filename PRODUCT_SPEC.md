# Product Spec
MVP includes landing page, authenticated shell via tenant header, evidence CRUD, upload, categories, expiry tracking, reminder run endpoint, proofpack JSON export, Stripe checkout/portal/webhook, and health endpoints.
