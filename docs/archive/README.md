# Archive

Superseded or consolidated documents are stored here to preserve institutional knowledge without keeping multiple active sources of truth.

- `root-legacy/` contains prior root-level markdown consolidated into canonical docs.
- Use archived docs for historical context only; do not treat them as active product truth.
